In order of appearance:

repeat a: a ⇒
	a

conjunct a b: a, b ⇒
	a ∧ b

unConjunctL a b: a ∧ b ⇒
	a

unConjunctR a b: a ∧ b ⇒
	b

contradiction a: a, ¬a  ⇒
	⊥

can't a: a → ⊥ ⇒
	¬a

can'tNot a: ¬a → ⊥ ⇒
	a
